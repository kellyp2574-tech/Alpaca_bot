# Combined Overnight Rebound Bot — runtime dependencies
#
# Lower bounds reflect the API surface we use today; upper bounds
# left open intentionally so security patches install cleanly.

# Alpaca REST + Massive HTTP
requests>=2.28

# Alpaca trade_updates websocket (FillStream)
websockets>=12.0

# .env loader for ALPACA_* / MASSIVE_API_KEY
python-dotenv>=1.0

# ── Test-only ────────────────────────────────────────────────
# Used by test_audit_fixes.py. Safe to omit in production deploys.
pytest>=8.0
